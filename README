i3po dictionary tool for i2b2
----------------------------

(Give some cool explanation here)

Deployment:
	Currently using Axis2 via Tomcat. To deploy, go to axis2 directory of
	/bin and use jar -cvf DefinitionService.aar ./* to create the service.
	Move the DefinitionService.aar to <APACHE_HOME>/webapps/axis2/WEB-INF/
	services/. Copy all .class files in /bin to appropriate places in
	directory structure under /opt/apache-tomcat-7.0.4/webapps/axis2/WEB-INF/
	classes/. Restart Tomcat; services should be up. You can test the
	current service by running the java test (TestDefinitionClient.java)

